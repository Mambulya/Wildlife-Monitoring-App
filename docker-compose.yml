name: wildlife-monitoring-app

services:
  backend:
    build: .
    container_name: backend_container
    command: uvicorn app.backend.main:app --reload --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"

    
  frontend:
    build: .
    container_name: frontend_container
    command: streamlit run app/frontend/monitoring_interface.py --server.port 8501 --server.address 0.0.0.0
    ports:
      - "8501:8501"
    depends_on:
      - backend
